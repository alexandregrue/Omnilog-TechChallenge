{% extends 'base.html.twig' %}

{% block title %}Note{% endblock %}

{% block body %}
    <h1>Note</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ note.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ note.text }}</td>
            </tr>
            <tr>
                <th>Début</th>
                <td>{{ note.startDate ? note.startDate|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ note.endDate ? note.endDate|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('note_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('note_edit', {'id': note.id}) }}" class="btn btn-primary">edit</a>

    {{ include('note/_delete_form.html.twig') }}

    <h2>Document :</h2>

    {% for document in note.documents %}
    <div>
        <img src="{{ asset('/uploads/' ~ document.name) }}" alt="Document">
    </div>
    {% endfor %}
{% endblock %}
